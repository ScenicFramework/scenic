# defmodule Scenic.Utilities do

#   #----------------------------------------------------------------------------
#   def smaller_to_bigger(smaller, bigger) do
#     cond do
#       smaller <= bigger -> {smaller, bigger}
#       true -> {bigger, smaller}
#     end
#   end

#   #----------------------------------------------------------------------------
#   def rand_string( str_length ) do
#     key = :crypto.strong_rand_bytes( 32 )
#     String.slice( Base.encode64(key), 0 .. (str_length - 1) )
#   end

# end
